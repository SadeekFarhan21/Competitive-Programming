#include <bits/stdc++.h>using namespace std;#define int long long#define double long double int32_t main() {    vector<int> answer;    vector<int> array;    for (int i = 0; i < 5; i++) {        array.push_back(4);        array.push_back(7);        sort(array.begin(), array.end());        do {            string s = "";            for(auto d : array) s += to_string(d);            int number = stoll(s);            answer.push_back(number);        } while (next_permutation(array.begin(), array.end()));    }    sort(answer.begin(), answer.end());    int n;    cin >> n;    auto it = lower_bound(answer.begin(), answer.end(), n);    if (it != answer.end()) {        cout << *it << "\n";        return 0;    }    cout << -1 << "\n";}