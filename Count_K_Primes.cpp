#include <bits/stdc++.h>
using namespace std;
#define int long long
#define double long double

/* 
TODO: Solution --> find the prime factorization of each number and that's the key
TODO: then we have a 2D array and if the 

*/

int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t; cin >> t;
    for(int i = 0; i < t; i++){
        solve();
    }
}