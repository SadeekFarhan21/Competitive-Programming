#include <bits/stdc++.h>using namespace std;#define int long long#define double long double int32_t main() {    ios::sync_with_stdio(false);    cin.tie(0);    cout.tie(0);    int n;    cin >> n;    vector<pair<string, string>> m;    for (int i = 0; i < n; i++) {        string a, b;        bool found = false;        cin >> a >> b;        for (auto e : m) {            if (a == e.first && b == e.second) {                found = true;                break;            }        }        if (!found)            m.push_back({a, b});    }    cout << m.size() << "\n";    return 0;}