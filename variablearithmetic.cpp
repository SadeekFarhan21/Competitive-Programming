#include <bits/stdc++.h>
using namespace std;

int main(){
    while(!cin.eof()){
        string s; getline(cin, s);
        map <string, int>
        vector <string> v;
        for(int i = 0; i < s.size(); i++){
            string x = "";
            if(s[i] == '+'){
                v.push_back(x);
            }
            else {
                x.push_back(s[i]);
            }
        }
    }
    return 0;
}