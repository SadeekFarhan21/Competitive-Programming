#include "bits/stdc++.h"
using namespace std;
#define int long long
#define double long double

int32_t main(){
    int n; cin >> n;
    vector <int> v(n);
    vector <int> cost(n + 1);
    for(int i = 0; i < n; i++){
        cin >> v[i];
    }
    cost[0] = 0;
    for(int i = )
    return 0;
}