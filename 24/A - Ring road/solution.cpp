#include <bits/stdc++.h>using namespace std; int cost[105][105];vector<int> adj[105];bool visited[105]; int main() {    int n;    cin >> n;     int total_cost = 0;    for (int i = 0; i < n; i++) {        int u, v, c;        cin >> u >> v >> c;        cost[u][v] = 0;        cost[v][u] = c;         adj[u].push_back(v);        adj[v].push_back(u);        total_cost += c;    }     int total_flip_cost = 0;    int curr = 1;    int prev = -1;     for (int i = 0; i < n; i++) {        int next_node = (adj[curr][0] == prev) ? adj[curr][1] : adj[curr][0];         total_flip_cost += cost[curr][next_node];         prev = curr;        curr = next_node;    }     cout << min(total_flip_cost, total_cost - total_flip_cost) << endl;     return 0;}