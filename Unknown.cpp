#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    ll t; cin >> t;
    vector <string> v(t);
    for(ll i = 0; i < t; i++){
        cin >> v[i];
    }
    string name; cin >> name;
    ll a, p; cin >> a >> p;
    while(p == 0){
        
    }

}